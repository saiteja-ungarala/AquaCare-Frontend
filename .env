# API Base URL for Expo
# Change this to your laptop's LAN IP address
# Note: When using Expo tunnel mode, the phone cannot access LAN IPs directly
# Use --lan mode instead: npx expo start --lan

EXPO_PUBLIC_API_URL=http://192.168.1.8:3000/api
